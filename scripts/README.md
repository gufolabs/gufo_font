This directory contains utility scripts for Gufo Fonts.

* `build` - Build CSS and create resulting bundle in `build/` directory.
* `make-codepoints.py` - Compile `manifest.yml` to `build/_codepoints.scss`.
* `post-process.py` - Post-processing, hash font's url if necessary.